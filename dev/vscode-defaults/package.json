{
  "name": "vscode-defaults",
  "displayName": "VS Code Defaults",
  "description": "Container defaults that must be available before extension host startup.",
  "version": "0.0.1",
  "publisher": "local",
  "private": true,
  "engines": {
    "vscode": "^1.90.0"
  },
  "categories": [
    "Other"
  ],
  "contributes": {
    "configurationDefaults": {
      "extensions.experimental.affinity": {
        "vscodevim.vim": 1
      }
    }
  }
}
